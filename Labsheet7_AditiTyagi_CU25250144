'''import heapq
heap = []
heapq.heappush(heap, 30)
heapq.heappush(heap, 10)
heapq.heappush(heap, 50)
heapq.heappush(heap, 20)
print("Min Heap:", heap)
print("Priority Queue:")
while heap:
    print(heapq.heappop(heap), end=" ")
arr = [40, 10, 30, 50, 20]
heapq.heapify(arr)
sorted_arr = []
while arr:
    sorted_arr.append(heapq.heappop(arr))
print("\nHeap Sort:", sorted_arr)'''
'''arr = [4,5,8,9,11]
arr.sort()
n = len(arr)
result = []
i, j = 0, n - 1
while i <= j:
    if i <= j:
        result.append(arr[j])
        j -= 1
    if i <= j:
        result.append(arr[i])
        i += 1
total = 0
for k in range(n - 1):
    total += abs(result[k] - result[k + 1])
print("Rearranged Array:", result)
print("Maximum Sum:", total)'''
arr = [2, 3, 1, 2, 4, 3]
target = 7
start = 0
total = 0
minimum = len(arr) + 1

for end in range(len(arr)):
    total += arr[end]

    while total > target:
        minimum = min(minimum, end - start + 1)
        total -= arr[start]
        start += 1

if minimum == len(arr) + 1:
    print(-1)
else:
    print("Smallest Subarray Length:", minimum)
